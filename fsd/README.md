Lab3 — React + TypeScript + Tailwind (FSD skeleton)

Run locally:

1. Install dependencies:

```bash
cd lab3_fsd
npm install
```

2. Start dev server:

```bash
npm run dev
```

App will open at http://localhost:3000
